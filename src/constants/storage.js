/**
 * LocalStorage (and other client storage) keys.
 * Use these constants to avoid typos and enable consistent key management.
 */
export const STORAGE_KEYS = {
  TOKEN: 'token',
  REFRESH_TOKEN: 'refreshToken',
};
